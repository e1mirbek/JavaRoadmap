

// Область видимости (scope)


public class TwentyFourthLs {

    // Область видимости определяет где в коде доступна переменная или метод 

    // Виды областей видимости:
    // 1. Локальная область видимости (Local Scope)
    // 2. Блочная область видимости (Block Scope)
    // 3. Параметры метода (Method Parameter Scope)
    // 4. Область видимости переменных класса (Class / Instance Scope)


    // 1. Локальная область видимости (Local Scope)
    // 👇👇👇

    public static void main(String[] args) {


        int x = 10; // локаольная переменная 
        System.out.println(x); // доступна здесь 

    }

    // x - здесь это переменная уже недоступна 


    // 2. Блочная область видимости (Block Scope)

    // Если переменная объявлена внутри фигурных скобок {}, она живёт только внутри этих скобок

    // 👇👇👇

    if (true) {
        int y = 20; // блочная переменная 
        System.out.println(y); // доступна тут
    }

    // здесь переменная y ошибка так как это блочная область видимости !!!


    // 3. Параметры метода (Method Parameter Scope)

    // Параметры метода существуют только во время выполнения метода.

    // 👇👇👇

    public static void printNumber (int num) { // num доступен только в этом методе 
        num = 25;
        System.out.println(num);
    }

    // 4. Область видимости переменных класса (Class / Instance Scope)

    // Переменные, объявленные внутри класса, но вне методов, называются полями (fields).

    // Instance variable (поле объекта) — доступно для каждого объекта класса.

    // Static variable (поле класса) — общее для всех объектов.

    // 👇👇👇

    int instanceVar = 100; // поле обьекта 
    static int staticVar = 200; // поле класса 

    public void show () {
        System.out.println(instanceVar); // Доступно внутри методов 
        System.out.println(staticVar); // Доступно внутри методов 
    }


    // 👉 Переменная "живёт" только в том месте, где она объявлена.
    //👉 Как только программа выходит из этой области (метода, блока, цикла), переменная удаляется из памяти.





}