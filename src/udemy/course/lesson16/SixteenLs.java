package udemy.course.lesson16;

// приоритет операций

// Приоритет операций — это порядок, в котором выполняются операторы в выражении.
// Операторы с более высоким приоритетом выполняются раньше, чем операторы с низким.

public class SixteenLs {
    public static void main(String[] args) {

        System.out.println("2 + 2" + 2 + 2);
        System.out.println(2 + 2 + " = 2 + 2");
        System.out.println("abc " + 1 + 2);

        // чтобы изменить порядок операций

        System.out.println("2 + 2 > " + (2 + 2));

        // Сначала выполняется умножение (3 * 4 = 12), потом сложение (2 + 12 = 14).
        // 👉 Потому что * имеет более высокий приоритет, чем +.

        System.out.println(2 + 3 * 4);

        // 🔹 Операторы && и ||

        // && (логическое И) — истина (true), если оба условия true.

        // || (логическое ИЛИ) — истина, если хотя бы одно условие true.

        System.out.println(true && false);
        System.out.println(true || false);

        // true && false
        // Левое значение: true
        // Правое значение: false
        // Оператор && требует, чтобы оба были true.
        // 👉 Результат: false
        // Вторая строка:
        // true || false
        // Левое значение: true
        // Правое значение: false
        // Оператор || сработает, если хотя бы одно true.
        // 👉 Результат: true

        System.out.println(true || false && false || true);
                          // (красный или зеленый и средний или большой)

    }
}

